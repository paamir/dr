@using _0_Framework.Application
@model string

@if (!string.IsNullOrWhiteSpace(Model))
{
	var statusMessageClass = Model.Contains(OperationMessages.SuccessKey) ? "success" : "danger";
	<div id="Message" class="alert alert-@statusMessageClass alert-dismissible" role="alert">
		@Model
	</div>
}